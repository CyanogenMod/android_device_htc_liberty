Device configuration for the HTC Pico (aka HTC Explorer)

* Description

  This repository is for CM7 on HTC Pico
  It is still a work in progress.


* How To Build CM7 for HTC Pico

  Make a workspace

  $ cd ~
  $ mkdir android
  $ cd android
  $ mkdir system
  $ cd system


  Do repo init & sync

  $ repo init -u git://github.com/CyanogenMod/android.git -b gingerbread
  $ repo sync -j20

  Setup vendor

  $ cd vendor/cyanogen/
  $ ./get-rommanager


  Setup device

  Connect phone to usb abd make sure debugging is avaliable for adb pulls

  $ cd ../../device/htc/pico/
  $ ./extract-files.sh # this pulls files from working liberty with most variants of shipped HTC ROM
  $ cd ../../..


  Setup environment

  $ source build/envsetup.sh
  $ lunch
  (select cyanogen_pico-eng)


  Build CM7

  $ make acp
  (to avoid errors on distributed building)

  $ make -j4 bacon


* Thanks

  Cyanogen
  attn1
  jznomoney (who booted the first liberty on Gingerbread and contributes tons to this device)
  CyanogenMod team
  sakindia123

----
EOF
